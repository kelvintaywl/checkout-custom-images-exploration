version: 2.1

jobs:
  echo:
    docker:
      - image: circleci/cci-demo-docker-primary:0.0.1
        auth:
          username: $DOCKER_LOGIN
          password: $DOCKER_PASSWORD
    steps:
      - checkout:
          path: C:\source
      - run:
          command: |
              tree C:\source

workflows:
  main:
    jobs:
      - echo
